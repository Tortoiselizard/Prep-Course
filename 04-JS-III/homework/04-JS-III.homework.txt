1. En un archivo de texto separado que debes crear, escribe explicanciones de los siguientes conceptos como si se lo estuvieras explicando a un niño de 12 años. Hacer esto te ayudará a descubrir rápidamente cualquier agujero en tu comprensión.

	Arrays: los arrays son una lista de elementos. Esos elementos pueden ser numeros, cadena de caracteres, booleanos, etc. Estas listas están ordenadas para que si preguntas ¿qué elementos hay en la posición 5 (por ejemplo)? te devuelva el valor que esta guardado ahí (en caso de existir).

2. Desde la carpeta Prep en la carpeta donde clonaste el repo, ingresa el comando npm test JSIII.test.js para correr los tests automatizados. Al principio, todos los tests estarán fallados/rotos. Encontrarás las funciones para hacer pasar los tests en el archivo homework.js.